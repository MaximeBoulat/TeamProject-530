# Build artifacts paths
BUILD_ROOT = "build"

# Datasets
DATASETS_ROOT = f"{BUILD_ROOT}/datasets"

# Model artifacts
MODELS_ROOT = f"{BUILD_ROOT}/models"
RESULTS_ROOT = f"{BUILD_ROOT}/results"
PLOTS_ROOT = f"{BUILD_ROOT}/plots"